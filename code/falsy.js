/**
 * Falsy values:
 *
 * false
 * null
 * undefined
 * 0
 * NaN
 * ''
 *
 */

const falsy = 0;
if (falsy) {
  // unreachable if block
}

// Truthy is everything else
// including -1, '0', 'false', [] or {}
